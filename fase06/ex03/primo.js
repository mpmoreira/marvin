function primo(x) {
  if (x===2 || x===3 || x===5 || x===7 || x===11)
    return "Sim"

    if (x%2===0 || x%3===0 || x%5===0 || x%7===0 || x%11===0)
    return "Não"

    else 
        return "Sim"
        
}
